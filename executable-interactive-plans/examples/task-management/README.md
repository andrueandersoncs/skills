# Task-management executable story tests

This example starts from `../../assets/story-test-template` and replaces only plan data, Effect Arbitrary tests, and proposed domain code.

```sh
npm install
npm run dev
```

Open `http://127.0.0.1:4174/`.
