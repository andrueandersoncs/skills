# Effect context

Apply this guidance as context to the one software workflow selected by the `software-craft` router whenever the project imports `effect` or `@effect/*`.

1. Read `node_modules/effect/AGENTS.md` and follow it.
2. Resolve its `./ai-docs/` links to `node_modules/effect/ai-docs/`.
3. Read APIs from `node_modules/effect/src`.
4. Read companion packages from `node_modules/@effect/*`.

The installed package is authoritative. This reference does not own routing, sequencing, or completion.
